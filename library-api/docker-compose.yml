version: '3'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - MONGODB_URI=mongodb+srv://Scorhenao:Samuelpro.2006@library.boubc.mongodb.net/?retryWrites=true&w=majority&appName=library
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
